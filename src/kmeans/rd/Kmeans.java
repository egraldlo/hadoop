package kmeans.rd;

import java.io.IOException;

public class Kmeans {
	public static void main(String[] args) throws IOException,ClassNotFoundException,InterruptedException{
		//很重要的Configuration类，这个类中，可以不根据磁盘上设定的来修改Configuration的设置
		//还有一个很重要的就是Configuration是以job为粒度的，每一个job是对应一个conf
	}
}
